import React from 'react'
// import { makeStyles} from '@material-ui/core';

export const LinkButton = ({titulo}) => {
    
   // const classes = useStyles();

    return (
        <div>
            {titulo}
        </div>
    )
}
// const useStyles = makeStyles((theme) => ({
   
// }));